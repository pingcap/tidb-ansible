---
# Tasks to configure firewalld rules

- name: All enabled portes
  debug: var=firewalld_ports

- name: Enable firewalld ports
  firewalld:
    port: '{{item}}'
    permanent: true
    state: enabled
  when: has_firewalld
  with_items: "{{ firewalld_ports }}"
  notify: reload firewalld
