---

- name: add crontab
  cron:
    name: "tidb-{{ tidb_port }}"
    user: "{{ ansible_user }}"
    minute: 0
    state: present
    job: 'find {{ deploy_dir }}/log -type f -name "tidb.log*" -mtime +{{ log_retain_days }} -exec rm -f {} \;'
