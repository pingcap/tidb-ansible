apiVersion: 1
providers:
  - name: {{ cluster_name }}
    folder: {{ cluster_name }}
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: {{ grafana_dashboards_dir }}/{{ cluster_name }}
