apiVersion: v1
kind: Service
metadata:
  name: alertmanager-monitoring
  namespace: kube-system
  labels:
    k8s-app: alertmanager-monitoring
    kubernetes.io/cluster-service: "true"
    kubernetes.io/name: "alertmanager"
spec:
  selector:
    k8s-app: alertmanager-monitoring
  ports:
    - port: 9093
      protocol: TCP
      targetPort: ui
